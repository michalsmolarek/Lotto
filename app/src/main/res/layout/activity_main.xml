<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#fff"
    tools:context="io.github.michalsmolarek.lotto.MainActivity">

    <!-- Tworzę element Textview, który pokaże nam liczby -->

    <TextView
        android:id="@+id/textView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:paddingBottom="20dp"
        android:paddingTop="30dp"
        android:text="@string/naci_nij_poni_szy_przycisk"
        android:textSize="20sp"
        />

    <!--Obrazek-->

    <ImageView
        android:id="@+id/imageView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/lotto"
        android:layout_above="@id/textView"
        android:layout_centerInParent="true"
        android:contentDescription="@string/logo_lotto" />

    <!--guzik-->
    <Button
        android:id="@+id/button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/textView"
        android:layout_centerInParent="true"
        android:text="@string/generuj_liczby"
        android:textSize="17sp"
        android:padding="20dp"
        />




</RelativeLayout>
